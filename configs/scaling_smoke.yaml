defaults:
  - scaling
  - override model_size: tiny
  - _self_

scaling:
  target_tokens: 1000

optimization:
  lr: 0.0003

evaluation:
  max_eval_batches: 2

hydra:
  sweep:
    dir: out/scaling_smoke
